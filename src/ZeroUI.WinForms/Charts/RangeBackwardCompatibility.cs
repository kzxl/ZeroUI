using System;

namespace ZeroUI.WinForms.Range
{
    // =========================================================================================
    // BACKWARD COMPATIBILITY SHIMS
    // These type declarations ensure that existing code referencing ZeroUI.WinForms.Range
    // continues to compile without breaking changes.
    // =========================================================================================

    [Obsolete("Use ZeroUI.WinForms.Charts.RangeControl instead.")]
    public class RangeControl : ZeroUI.WinForms.Charts.RangeControl { }
}
